# Example environment variables needed for the payment pipeline
STRIPE_SECRET_KEY=sk_test_example
XERO_CLIENT_ID=example_client_id
XERO_CLIENT_SECRET=example_client_secret
XERO_REDIRECT_URI=https://payment-pipeline-example.com/callback
PANDADOC_API_KEY=example_api_key
FIREBASE_SERVICE_ACCOUNT={"type":"service_account","project_id":"example"}

# Run locally with:
# export $(cat .env | xargs) && node index.js
